# -----------------------------
# Example environment variables
# Copy this to `.env` and fill real values (DO NOT commit .env)
# -----------------------------

# MongoDB (use Atlas URI for production)
# Example (Atlas): mongodb+srv://<user>:<password>@cluster0.gdzrml4.mongodb.net/portfolio?retryWrites=true&w=majority
# Use the URI provided (replace <db_password> with the real password):
MONGODB_URI=mongodb+srv://admin:<9faZxbY5gcqrpnP4>@testing.gdzrml4.mongodb.net/?appName=Testing

# JSON Web Token (required for auth)
JWT_SECRET=replace_with_a_long_random_string
JWT_EXPIRES_IN=7d

# Server
PORT=5000
NODE_ENV=production

# CORS - frontend URL (allowed origin)
CLIENT_URL=http://localhost:3000

# Notes:
# - Keep real secrets out of source control; add them in Vercel/hosting provider for production.
# - For Vercel add MONGODB_URI, JWT_SECRET and NODE_ENV under Project -> Settings -> Environment Variables.
